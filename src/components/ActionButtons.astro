---
import { actionLinks } from '../config/profile';
---
<div class="grid gap-4 md:grid-cols-3">
	{actionLinks.map((action) => (
		<a
			class="group flex items-center gap-3 rounded-2xl border border-white/10 bg-slate-950/60 px-4 py-3 text-left transition hover:border-emerald-400/60"
			href={action.href}
			target={action.external ? '_blank' : undefined}
			rel={action.external ? 'noreferrer' : undefined}
		>
			{action.icon && (
				<span class="flex size-11 items-center justify-center rounded-2xl bg-white/5">
					<img
						src={action.icon}
						alt=""
						width="32"
						height="32"
						loading="lazy"
						class="h-8 w-8"
					/>
				</span>
			)}
			<div class="flex flex-col">
				<span class="text-base font-semibold text-white">{action.label}</span>
				{action.description && <span class="text-sm text-slate-300">{action.description}</span>}
			</div>
		</a>
	))}
</div>
