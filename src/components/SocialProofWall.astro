---
import { socialProofEntries } from '../config/profile';
---
<section class="rounded-3xl border border-white/5 bg-slate-900/30 p-8">
	<header class="mb-6 flex flex-col gap-2">
		<p class="text-sm uppercase tracking-[0.3em] text-slate-400">Receipts</p>
		<h2 class="text-3xl font-semibold text-white">Social proof that actually matters</h2>
		<p class="text-slate-300">Screenshots coming soon. Until then, a few direct quotes.</p>
	</header>
	<div class="grid gap-4 md:grid-cols-3">
		{socialProofEntries.map((proof) => (
			<article class="rounded-2xl border border-white/5 bg-slate-950/40 p-5">
				<p class="text-base text-slate-100">“{proof.quote}”</p>
				<div class="mt-4 text-sm text-slate-400">
					<p class="font-semibold text-slate-200">{proof.author}</p>
					{proof.title && <p>{proof.title}</p>}
				</div>
			</article>
		))}
	</div>
</section>
